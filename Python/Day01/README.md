# Day 1

## 공부한 내용

1. 파이썬 소개
  (1) Python?  읽고 쓰기 쉽게 디자인된 언어. 
  (2) 풍부한 라이브러리 : 문법은 쉬우나 목적에 맞는 라이브러리를 잘알아야한다.
  (3) 파이썬 설치

2. 데이터형과 데이터구조
   (1) 연산 : IDLE은 계산기 대신 이용이 가능 --> 사칙연산(+-*/), 나머지(%), 몫과 나머지[divmod(,)], 몫을 정수(//), 지수(**)
   (2) 변수 : 대소문자, 숫사, 언더바(_)로 변수명 설정가능, 숫자가 처음에 오는것은 불가
   (3) 대입의 간이 기법 : a += 1(a = a+1)

3. 함수 : 여러 줄의 코드를 하나의 이름으로 묶어서, 필요할 때마다 재사용할 수 있게 만든 것

    (1) 종류 : 내장함수 - 파이썬에 이미 존재하는 함수
               사용자 정의 함수 - 직접 만든 함수
   
    (2) 종류 : 메서드- 특정요소(객체)가 있어야 호출이 가능 Ex) 'scores.append(79)'
                메서드 X : 그냥 호출이 가능

5. 데이터형 : 다룰 수 있는 기본적인 데이터 종류 -> 정수, 부동소수점, 문자열, 부울값
     (1) [데이터형 판독] : type(6) 입력시 int출력
     (2) [문자열] : '' or "" 사용
     (3) [부울값] : True or False로 구분

6. 형변환 : 데이터를 다른 형으로 변환
    
## 배운 점

C언어와는 다르게 세미콜론(;)이 포함되지 않는 다는 것을 알게 되었다.

## 코드

practice.py 참고
