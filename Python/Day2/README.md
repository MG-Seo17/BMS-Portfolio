</> Markdown

# Day 2

## 공부한 내용

1. 리스트(뮤터블) : 0개 이상의 요소를 가지는 시퀀스. 요소를 추가 혹은 삭제 가능하다. [ , ]로 표현

2. 튜플(이뮤터블) : 0개 이상의 요소를 갖는 시퀀스. 요소를 추가 혹은 삭제 혹은 수정이 불가능. ( , )로 표현

3. 사전 : 조사하는 단어를 [키], 그 값을 [밸류]라고 표현, { }으로 키와 밸류를 결정

    (1) 종류 : 내장함수 - 파이썬에 이미 존재하는 함수
               사용자 정의 함수 - 직접 만든 함수
   
    (2) 종류 : 메서드- 특정요소(객체)가 있어야 호출이 가능 Ex) 'scores.append(79)'
                메서드 X : 그냥 호출이 가능

4. 리스트의 리스트 구조: 리스트 안에 리스트가 존재하는 구조...배열과 비슷하다

## 배운 점

리스트와 튜플이 기능은 같으나 중간에 수정할 수 있는것이 리스트이기에 둘을 헷갈리면 안되겠다고 생각을 했다.
그리고 굳이 튜플이 있는 이유가 궁금했는데 코딩을 하다보면 절대 바뀌면 안되는 값이 존재하는데 이를 튜플로 설정하면 된다는 것을 알게되어 좋았다.

## 코드

practice.py 참고

